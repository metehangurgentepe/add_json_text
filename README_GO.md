# Niobe Button Manager - Go Web Application

Modern, kullanıcı dostu bir web arayüzü ile Supabase button kayıtlarını yönetin ve SQL sorguları oluşturun.

## Özellikler

### Training Data Yönetimi
- 📝 Input/Output training data ekleme
- 🎲 Otomatik Action Key (UUID) oluşturma
- 💾 niobe_training.txt dosyasına kaydetme
- 🔗 Action Key'i direkt button formuna aktarma

### Button Yönetimi
- ✨ Modern ve responsive web arayüzü
- 💾 **Direkt Supabase'e kaydetme** (SSL güvenli)
- 🎲 Otomatik Button ID (UUID) oluşturma
- 📋 SQL sorgularını kopyalama
- 📥 Toplu SQL export (tüm buttonları indir)
- ⚡ Hızlı ve basit kullanım
- 🎨 Güzel ve kullanıcı dostu arayüz

## Kurulum

### Gereksinimler

- Go 1.21 veya üzeri

### Adımlar

1. **Go'yu yükleyin** (eğer yüklü değilse):
   ```bash
   # macOS
   brew install go

   # veya https://golang.org/dl/ adresinden indirin
   ```

2. **Bağımlılıkları yükleyin**:
   ```bash
   go mod download
   ```

3. **Uygulamayı başlatın**:
   ```bash
   go run main.go
   ```

4. **Tarayıcınızda açın**:
   ```
   http://localhost:8080
   ```

## Kullanım

### 🔄 Tam İş Akışı (Önerilen)

#### 1. Training Data Oluştur
1. **Input Text**: Kullanıcının soracağı soru (örn: "Evde Bakım")
2. **Description**: AI'ın vereceği yanıt
3. **Action Key**: Boş bırak ve "🎲 UUID Oluştur" butonuna tıkla
4. "➕ Training Data Ekle" butonuna tıkla
5. Kaydetme seçenekleri:
   - "📄 Dosyaya Kaydet" → `niobe_training.txt` dosyasına ekler
   - "💾 Supabase'e Kaydet" → `ai_log.instructions` tablosuna ekler

#### 2. Button Oluştur
1. "⬇️ Bu ID ile Button Oluştur" butonuna tıkla (Action Key otomatik gelir!)
2. **Button Title**: Butonun üzerinde yazacak metin
3. **Action Type**: route, url, phone, app_store
4. **Navigation Type**: push, go, pushReplacement
5. **Action Value**: Route path veya URL (örn: /form/164)
6. "✅ Button Ekle" butonuna tıkla
7. "💾 Supabase'e Kaydet" ile direkt veritabanına kaydet!

### 📋 Alternatif: Manuel SQL ile Ekleme

1. Button ekledikten sonra SQL görüntülenir
2. "📋 SQL'i Kopyala" butonuna tıklayın
3. Supabase SQL Editor'e yapıştırın ve çalıştırın

### SQL Kopyalama

- Her button eklendiğinde SQL sorgusu "SQL Çıktısı" bölümünde gösterilir
- "SQL'i Kopyala" butonuna tıklayarak panoya kopyalayabilirsiniz
- Kopyalanan SQL'i direkt Supabase SQL Editor'e yapıştırabilirsiniz

### Toplu Export

- Birden fazla button ekledikten sonra "Tüm Buttonları SQL Olarak İndir" butonuna tıklayın
- Tüm buttonlar için SQL sorguları içeren bir `.sql` dosyası indirilecektir

## Proje Yapısı

```
.
├── main.go              # Ana uygulama dosyası
├── templates/
│   └── index.html       # HTML template
├── static/
│   └── css/
│       └── style.css    # CSS stilleri
├── go.mod               # Go module dosyası
└── README_GO.md         # Bu dosya
```

## API Endpoints

- `GET /` - Ana sayfa
- `POST /add-button` - Yeni button ekle
- `POST /generate-sql` - SQL sorgusu oluştur
- `GET /api/buttons` - Buttonları listele (gelecekte Supabase entegrasyonu)

## Örnek Button Verileri

```json
{
  "id": "bb8b66ce",
  "title": "Başkana İlet",
  "action_type": "route",
  "navigation_type": "push",
  "action_value": "/form/164",
  "order": 0,
  "updated_at": "2026-01-17T17:38:11.250683"
}
```

## SQL Çıktısı Örneği

```sql
INSERT INTO public.button (id, title, action_type, navigation_type, action_value, "order", updated_at)
VALUES (
    'bb8b66ce',
    'Başkana İlet',
    'route',
    'push',
    '/form/164',
    0,
    '2026-01-17T17:38:11.250683'
);
```

## 📊 Veritabanı Şeması

### ai_log.instructions
```sql
create table ai_log.instructions (
  id bigint generated by default as identity not null,
  created_at timestamp with time zone not null default now(),
  instructions text null,
  constraint instructions_pkey primary key (id)
) tablespace pg_default;
```

### public.button
```sql
create table public.button (
  id text not null,
  title text,
  action_type text,
  navigation_type text,
  action_value text,
  "order" integer,
  updated_at timestamp with time zone,
  constraint button_pkey primary key (id)
) tablespace pg_default;
```

## ✅ Tamamlanan Özellikler

- [x] Training data oluşturma ve dosyaya kaydetme
- [x] Training data'yı Supabase'e kaydetme (`ai_log.instructions`)
- [x] Direkt Supabase button entegrasyonu (otomatik insert)
- [x] Action Key ile training data ve button entegrasyonu
- [x] SQL sorgusu oluşturma ve kopyalama
- [x] SSL güvenli bağlantı

## Gelecek Özellikler

- [ ] Mevcut buttonları listeleme ve düzenleme
- [ ] Button silme işlemi
- [ ] Training data görüntüleme (Supabase'den)
- [ ] Kullanıcı kimlik doğrulama

## Katkıda Bulunma

Bu proje Niobe Assistant projesi için geliştirilmiştir. Önerilerinizi ve katkılarınızı bekliyoruz!

## Lisans

Bu proje özel kullanım içindir.
